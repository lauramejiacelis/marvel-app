/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['upload.wikimedia.org', 'wallpapershome.com', 'i.annihil.us']
  }
}

module.exports = nextConfig
